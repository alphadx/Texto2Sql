name: demo-smoke

on:
  pull_request:
  workflow_dispatch:

jobs:
  demo-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run demo smoke test (build + sakila validation)
        run: make demo-smoke
